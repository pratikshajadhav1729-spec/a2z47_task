<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(270deg, #89f7fe, #66a6ff, #fbc2eb, #fad0c4, #a1c4fd, #c2e9fb);
      background-size: 1200% 1200%;
      animation: gradientBG 16s ease-in-out infinite;
      transition: background 0.5s;
    }
    @keyframes gradientBG {
      0% {background-position: 0% 50%;}
      25% {background-position: 50% 100%;}
      50% {background-position: 100% 50%;}
      75% {background-position: 50% 0%;}
      100% {background-position: 0% 50%;}
    }
    .container-box {
      background: rgba(255,255,255,0.92);
      padding: 48px 38px;
      border-radius: 22px;
      box-shadow: 0px 10px 32px rgba(0, 0, 0, 0.18);
      text-align: center;
      max-width: 420px;
      width: 92%;
      animation: fadeIn 1.2s cubic-bezier(.39,.575,.56,1.000);
      backdrop-filter: blur(2px);
    }
    h1 {
      font-size: 2.6rem;
      font-weight: 800;
      margin-bottom: 18px;
      color: #222;
      letter-spacing: 1px;
      text-shadow: 0 2px 8px rgba(102,166,255,0.08);
    }
    p {
      font-size: 1.08rem;
      color: #555;
      margin-bottom: 32px;
    }
    .btn-custom {
      border-radius: 30px;
      padding: 13px 32px;
      font-size: 18px;
      margin: 12px 8px;
      font-weight: 600;
      letter-spacing: 0.5px;
      box-shadow: 0 2px 12px rgba(102,166,255,0.10);
      transition: all 0.25s cubic-bezier(.39,.575,.56,1.000);
    }
    .btn-custom:hover {
      transform: translateY(-5px) scale(1.07);
      box-shadow: 0px 10px 28px rgba(102,166,255,0.18);
      opacity: 0.95;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.92);}
      to { opacity: 1; transform: scale(1);}
    }
  </style>
</head>
<body>

  <div class="container-box">
    <h1>Welcome</h1>
    <p>Start your journey by registering a new account or logging in to continue.</p>
    <a href="/register" class="btn btn-primary btn-custom">Register</a>
    <a href="/login" class="btn btn-outline-dark btn-custom">Login</a>
  </div>

</body>
</html>
